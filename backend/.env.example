# Backend Environment Variables

# Server Configuration
NODE_ENV=development
PORT=3001

# Database Configuration
DB_PATH=./db/pos.db

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key

# Optional Remote APIs
# Remote price lookup API endpoint
REMOTE_PRICE_API=

# Remote catalog URL for price sync
REMOTE_CATALOG_URL=

# Price sync interval in milliseconds (default: 900000 = 15 minutes)
PRICE_SYNC_MS=900000

# File Upload Paths
UPLOADS_PATH=./uploads
IMAGES_PATH=./images

# CORS Configuration (optional)
CORS_ORIGIN=*

# Printer Configuration
# Set your printer interface here:
# - For default system printer: printer:default
# - For specific Windows printer: printer:PrinterName
# - For USB thermal printer: usb
# - For network printer: tcp://192.168.1.100:9100
PRINTER_INTERFACE=printer:default

# Optional: Set printer type for better compatibility
# Options: EPSON, STAR, DARUMA, DRAUMA
PRINTER_TYPE=EPSON